<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-3 sticky-top">
  <div class="container">
    <!-- Logo / Brand -->
    <a class="navbar-brand d-flex align-items-center fw-bold text-primary" href="/home">
      <img src="<%= pharmacy?.image || '/images/default-logo.png' %>" alt="Logo" width="35" height="35" class="me-2">
      <span class="fs-4"><%= pharmacy?.pharmacy_name || 'Pharmacy' %></span>
    </a>

    <!-- Toggler for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
      aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar links -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= url === '/' ? 'active text-primary fw-semibold' : '' %>" href="/home">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === '/store' ? 'active text-primary fw-semibold' : '' %>" href="/home/store">Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === '/categories' ? 'active text-primary fw-semibold' : '' %>" href="/home/categories">Categories</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === '/offers' ? 'active text-primary fw-semibold' : '' %>" href="/home/offers">Offers</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === '/contact' ? 'active text-primary fw-semibold' : '' %>" href="/home/contact">Contact</a>
        </li>
      </ul>

    <form class="d-flex me-lg-3 my-2 my-lg-0" role="search" action="/home/search-result" method="GET">
  <input class="form-control rounded-pill me-2" type="search" name="search" placeholder="Search medicines..."
    aria-label="Search" value="<%= searchQuery || '' %>">
  <button class="btn btn-primary rounded-pill px-3" type="submit">
    <i class="bi bi-search"></i>
  </button>
</form>


      <!-- Cart + Login -->
      <div class="d-flex align-items-center">
        <button type="button" class="btn btn-outline-primary rounded-pill me-2 position-relative" data-bs-toggle="modal"
          data-bs-target="#cartModal">
          <i class="bi bi-cart3"></i>
          <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
            <%= session?.cart?.length || 0 %>
          </span>
        </button>

        <a href="/login" class="btn btn-primary rounded-pill px-3">
          <i class="bi bi-person"></i> Login
        </a>
      </div>
    </div>
  </div>
</nav>

<!-- Custom Navbar Styles -->
<style>
  .navbar-nav .nav-link.active {
    font-weight: 600;
    color: #0d6efd !important;
  }
</style>
